---
import Footer from "@/components/astro/Footer.astro";
import Header from "@/components/astro/Header.astro";
import Router from "@/components/astro/Router.astro";
import { globalSettings } from "@/scripts/queries";
import "@/styles/global.css";

interface Props {
	title?: string;
}

const { title } = Astro.props;

if (!globalSettings[0]) {
	throw new Error("Global settings not found");
}

const { globalTitle } = globalSettings[0];

const pageTitle = title ? `${title} | ${globalTitle}` : globalTitle;
---

<!doctype html>
<html lang="en" transition:animate="none">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<meta name="description" content="Novo Holdings Annual Report 2024" />
		<Router />
		<title>{pageTitle}</title>
	</head>
	<body class="min-h-screen bg-white">
		<Header {...globalSettings[0]} />
		<main class="w-full">
			<div class="flex flex-col max-w-[100rem] mx-auto">
				<slot />
			</div>
		</main>
		<Footer {...globalSettings[0]} />
		<div id="portal" class="z-2 absolute w-0 h-0"></div>
	</body>
</html>

<script>
	import "@/scripts/lenis";
</script>
