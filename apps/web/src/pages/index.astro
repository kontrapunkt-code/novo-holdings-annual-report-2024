---
import { globalSettings } from "@/scripts/queries";

// Ensure homepage has a valid slug
if (!globalSettings[0]?.homePageSlug) {
	return Astro.redirect("/404");
}

// Redirect to the homepage with proper slug
return Astro.rewrite(`/${globalSettings[0].homePageSlug}`);
---
