---
import Button from "@/components/astro/Button.astro";
import Icon from "@/components/astro/Icon.astro";
import Link from "@/components/astro/Link.astro";
import { MagneticHover } from "@/components/solid/magnetic-hover";
import type { GlobalSettings } from "@/scripts/queries";

type Props = GlobalSettings;

const { footer } = Astro.props;
---

<footer class="bg-blue-grey mt-24 w-full pt-12 text-white">
	<div
		class="mx-auto grid w-full max-w-[100rem] gap-[12.5rem] px-[1.5rem] py-[1.5rem] md:px-[3rem] md:pb-[2.5rem]"
	>
		<div class="flex flex-wrap items-start justify-between gap-8 text-white">
			<div class="text-[2rem] leading-[90%] md:text-[4rem]">
				<Icon icon="logo" class="aspect-[257/122] h-28" />
			</div>
			<MagneticHover
				client:load
				pressable
				hoverActiveScale={1.05}
				moveStrength={{ x: 10, y: 5 }}
			>
				<Button link={footer?.backlink} icon="arrow_outward" />
			</MagneticHover>
		</div>
		<div class="space-y-12">
			<nav>
				<ul
					class="grid max-w-[100rem] gap-4 text-[0.875rem] leading-[120%] font-medium md:flex md:gap-[2rem]"
				>
					{
						footer?.links?.map((link) => (
							<li>
								<Link link={link} class="text-white hover:underline">
									{link.text}
								</Link>
							</li>
						))
					}
				</ul>
			</nav>
			{
				footer?.copyright && (
					<p class="max-w-4xl text-sm leading-[120%]">{footer?.copyright}</p>
				)
			}
		</div>
	</div>
</footer>
